export const PAGE_KEYS = ["posts", "diary", "friends", "excerpts", "thoughts", "guestbook", "resume", "calendar"];

export const LOGIN_MODE_OPTIONS = ["disable", "enable", "force"] as const;
export const AVATAR_STRATEGY_OPTIONS = ["identicon", "gravatar", "library"] as const;
export const MIGRATION_STATE_OPTIONS = ["draft", "syncing", "ready", "done"] as const;
