# Serino 协作约定

## 开发时优先复用

- 动手前，先搜索已有的函数、组件、工具方法、类型、样式和测试辅助代码。
- 能复用就复用；只有现有实现确实不符合需求时，才新增函数或组件。
- 不要为了一个页面或一次调用复制粘贴相似逻辑。需要在多处使用的逻辑，应提炼为名字清楚、职责单一的公共实现。
- 新增组件或函数前，确认它不会与现有实现职责重叠；优先扩展现有组件的合理接口。

## 技术选型与资料核验

- 使用符合当前标准、维护活跃且与项目兼容的最新稳定版技术库、API 和开发规范；不要沿用已弃用的写法，也不要无依据地采用测试版或不兼容的大版本。
- 在引入或修改算法、函数、技术库和实现方案前，优先使用 Context7 核验最新官方文档、推荐用法、版本兼容性和弃用信息。
- Context7 未覆盖或信息不足时，必须联网搜索，并优先参考官方文档、标准规范、官方发布说明、权威论文或维护者的原始资料；不要只凭记忆或不明来源的代码片段做决定。
- 选型时同时评估正确性、安全性、性能、维护状态、许可证、包体积和现有项目兼容性。需要新增依赖或升级版本时，说明采用的版本、原因与影响。

## 每一次修改的质量底线

- 性能：避免不必要的请求、计算、渲染和资源加载；改动前后都要关注用户能感受到的速度与流畅度。
- 用户体验：操作路径要自然，反馈要及时，加载、空数据、失败和移动端等状态都应清楚易用。
- 前端视觉：遵循现有的视觉语言，注意排版、间距、层级、颜色、响应式布局和交互状态；页面既要好用，也要看起来完整、舒适。
- 正确性：先保证结果正确，再考虑性能优化。所有输入、边界条件、异常路径和数据状态都要被正确处理并验证。
- 缓存：缓存只能加速正确结果，不能改变结果。缓存键必须覆盖所有会影响结果的条件，并具备明确的失效策略；数据可能过期、缓存状态不确定或无法保证正确性时，必须回源或跳过缓存，绝不能为了命中缓存而返回错误、过期或不属于当前用户的数据。

## 计划文件与日常提交约束

- 所有设计、实施、排查、修复等计划文件必须放在项目根目录的 `tmp/` 中，不得放在 `docs/` 或其他会进入版本控制的目录。
- `tmp/` 中的计划文件仅供本地执行和核对，严禁暂存、提交、打包进 tag 或包含在 Release 中。提交前必须再次确认这些文件已被排除。
- 从上一个正式 Release 完成后到下一次 Release 流程开始前，所有功能、修复、测试、文档和配置改动都保留为未提交状态，不创建中间提交。
- 只有进入 Release 流程、Release 草稿获得请求方明确确认、完成格式化且全量检查通过后，才允许根据本次 Release 的完整改动重新整理和分组提交。
- 每次 Release 的提交分组都必须以最近一次正式 Release 为基线重新审视，覆盖基线之后的全部改动；不得直接沿用开发过程中的临时分组，也不得遗漏已经暂存、尚未暂存、未跟踪或意外提前提交的内容。

## 提交信息与提交后核验

- 提交信息中的分段和换行必须是实际换行字符；严禁把 `\n`、`\r\n` 或其他转义序列作为字面文本写入标题或正文来代替换行。
- 每次提交后必须执行 `git show -s --format=%B HEAD` 核对正文排版；如发现字面量转义序列、缺少空行或编号格式错误，必须立即 amend 后才能继续后续操作。

## 当请求方说“release”或表达发布意图

一旦请求方提出 `release`、发布或“提交并且 release”，立即启动下面的流程。发布范围不是“当前会话改了什么”，而是从最近一次正式 Release 对应的 tag 开始，到当前工作区为止的全部变化。

### 版本号规则：X.Y.Z

- **X（主版本）**：正式版本发布。X 的变更不能自行决定，必须先获得请求方明确确认后才能创建对应 tag 和 Release。
- **Y（功能版本）**：新增功能或较大的功能重构。可以根据本次实际改动自行决定递增 Y。
- **Z（修复版本）**：Debug、缺陷修复和其他小范围改动。可以根据本次实际改动自行决定递增 Z。
- 创建 tag 前，以当前最新发布版本为基准，只递增本次改动对应的数字位；未获明确确认时，绝不递增 X。

### 第一阶段：立即核查并提交 Release 草稿

此阶段只允许读取和分析，不得格式化文件、修改代码、创建或重写提交、打 tag、推送或创建远程 Release。

1. 确认最近一次实际发布成功的 Release 及其对应 tag，并以它作为唯一基线；不能仅凭本地最高版本 tag 推断。如果本地 tag、远程 tag 和远程 Release 不一致，先核实并说明差异。
2. 核查从基线到 `HEAD` 的全部已提交变化，同时核查已暂存、未暂存和未跟踪文件。计划文件无论位于 `tmp/` 还是误放在其他目录，都必须识别并排除出发布内容；误放在受版本控制目录的计划文件应在后续提交前移入 `tmp/` 或删除。
3. 按用户可感知的功能、问题修复、体验变化、配置或部署影响整理完整发布范围，并同时核对数据库迁移、生成文件、测试覆盖、兼容性、安全性和升级步骤。
4. 根据完整变化判断目标版本号，设计新的提交分组。每组只包含同一项功能或同一类修复，并覆盖从基线之后的全部内容，包括意外提前创建的提交。
5. 向请求方提供 Release 草稿，至少包含：发布基线、建议版本号、完整变化摘要、计划提交分组、检查与迁移说明、已知风险，以及按本文模板写成的 Release 文案。
6. 明确请求确认草稿。请求方确认前必须停在此处，不得进入后续流程。若请求方要求调整，则更新核查结果和草稿后再次确认。

### 第二阶段：确认后连续完成提交与发布

请求方明确确认 Release 草稿，即表示授权按照已确认范围继续执行格式化、检查、分组提交、创建 tag、推送和创建远程 Release；除非出现范围、版本或风险的实质变化，不再把这些步骤拆开等待重复授权，并应持续执行到远程 Release 创建并核验成功。

1. 先运行项目已经配置且适用于本次改动的自动格式化工具，覆盖所有能够自动格式化的变更文件。当前后端使用 `uv run --directory backend ruff format src/ tests/`；如果项目增加统一格式化脚本，应优先使用统一脚本。不得臆造命令，也不得仅为发布临时引入新的格式化依赖。
2. 格式化完成后执行全量检查：`pnpm run check`。它覆盖前后端测试、类型检查、代码检查、后端格式检查和前端构建。
3. 格式化或检查产生的新变化也属于本次发布范围。检查未通过时，先修复并从格式化步骤重新执行；在全量检查通过前，不要提交、打标签或发布。若无法修复，清楚说明失败原因和影响范围。
4. 如果修复检查问题导致用户可感知范围、版本号、迁移步骤、风险或 Release 文案发生实质变化，必须更新草稿并再次获得请求方确认后才能继续。
5. 全量检查通过后，以最近一次 Release tag 为基线，重新整理基线后的所有提交和当前工作区改动，再按已确认的功能分组创建提交。若需要重整尚未发布的本地提交，必须先创建可恢复的本地备份引用；若涉及改写已经推送或共享的历史，必须在草稿中说明，且未经明确授权不得强制推送。
6. 提交信息使用 Conventional Commits，并用普通中文说明改了什么。例如：

   ```text
   fix(backup): 加固备份同步恢复可靠性

   1、扩展备份同步测试，覆盖完整数据集、恢复原子性、速度阈值和后台 UI 易用路径。
   2、修复真实 SFTP mkdir/ls 批处理误判，增加批量 chunk 下载以提升恢复速度。
   3、为容器任意运行 UID 补 NSS 映射，避免 OpenSSH/SFTP 在生产安装形态下无法解析用户。
   ```

7. 每次提交后都按“提交信息与提交后核验”一节检查提交正文。全部提交完成后，再核对 `基线 tag..HEAD` 的完整差异与提交列表，确认内容无遗漏、分组准确、`tmp/` 计划文件未进入历史，并确认工作区不存在本次发布遗漏的非计划改动。
8. 按已确认版本创建 tag，推送目标分支和 tag，再使用已确认的 Release 文案创建远程 Release。最后核验远程分支、tag 与 Release 指向同一提交，Release 标题、版本号和正文均正确。
9. 任一步骤失败都先定位并解决问题，再从受影响的步骤继续；不得在提交、tag、推送或 Release 只完成一部分时声称发布完成。最终向请求方报告提交列表、检查结果、tag、推送结果和 Release 地址。

## Release 写法：让普通用户一眼看懂

Release 内容必须清晰、准确、通俗：只写已经确认的实际变化，说明用户在什么地方会看到什么变化、现在能做什么、问题在什么情况下已被解决。不要只罗列“模块”“接口”“联调”等技术词，也不要用模糊的“优化了”“修复若干问题”代替事实。下面的栏目按实际改动取用：没有新功能就删除“新功能”，没有提醒就删除“使用提醒”；不要为了凑模板而增加内容。

```markdown
## [产品名称] v[版本号] 更新说明

**发布时间：** [YYYY 年 MM 月 DD 日]

这次更新主要带来了 [一句话概括本次更新，例如：更顺畅的使用体验和几项实用的新功能]。

### ✨ 新功能

[没有特别新功能时，可以删除这一部分。]

#### 1. **[功能名称]**

   [用一句通俗的话说明功能。]

   你现在可以：

   - [用户可以完成什么]
   - [适合什么场景]
   - [能带来什么好处]

#### 2. **[功能名称]**

   [用一句通俗的话说明功能。]

   你现在可以：

   - [用户可以完成什么]
   - [适合什么场景]
   - [能带来什么好处]

### 🚀 体验优化

[没有特别体验优化时，可以删除这一部分。]

- 优化了 [页面或功能]，现在 [具体变得怎样，例如：打开速度更快]。
- 简化了 [操作流程]，使用起来更加方便。
- 调整了 [界面或提示]，内容更加清晰易懂。

### 🐛 问题修复

[没有特别问题修复时，可以删除这一部分。]

- 修复了 [某种情况下] 无法 [完成某项操作] 的问题。
- 修复了 [页面或功能] 偶尔显示异常的问题。
- 修复了一些可能影响使用体验的小问题。

### ⚠️ 使用提醒

[没有特别提醒时，可以删除这一部分。]

- 更新后需要 [重新登录 / 刷新页面 / 升级客户端]。
- [某项功能] 目前仅支持 [平台、用户范围或使用条件]。
- 原来的 [旧功能] 已调整为 [新的使用方式]。

### 🛠 开发者 / 运维说明

[仅在配置、部署、兼容性或升级步骤确有变化时保留这一部分；没有变化时直接删除。]

- [是否需要更新配置、执行迁移、重启服务或重新构建]。
- [兼容性、升级步骤、接口或数据结构变化及其影响范围]。

*感谢您的使用与反馈 ~*
```

写 Release 时，用实际页面名和实际操作替换方括号内容，删掉不适用的标题、提示和占位符。每一条都应让普通用户知道“哪里变了、怎么使用、会有什么影响”，并确保表述与实际行为一致。
